# container.list
# Minimal package list for container environments
# Format: package[(cli_name)] [| manager:name ...]
# No platform tags needed for container list

git
zsh
starship
ripgrep(rg)
fd | apt:fd-find dnf:fd-find
fzf
jq
neovim(nvim)