# Tmux entrypoint
# Load plugins before user keybindings so local overrides win over tmux-sensible defaults.

source-file ~/.tmux.d/00-options.conf
source-file ~/.tmux.d/90-plugins.conf
source-file ~/.tmux.d/10-keybindings.conf
source-file ~/.tmux.d/20-status.conf
