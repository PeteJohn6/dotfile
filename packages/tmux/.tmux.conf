# Tmux entrypoint
# Bootstrap modular config loading from ~/.tmux.d.

run-shell "for file in 00-options.conf 10-keybindings.conf 11-keytables.conf 90-plugins.conf 20-status.conf; do tmux -S '#{socket_path}' source-file \"$HOME/.tmux.d/\$file\"; done"
